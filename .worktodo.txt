P1

That is a brilliant addition. Spot instances are the holy grail of cloud FinOps. They can save an enterprise 60% to 90% on GPU compute costs, but they can be preempted (shut down) at any time.

This introduces a perfect, highly realistic AI safety scenario: The agent must understand the difference between a fault-tolerant workload (which should use Spot) and a production-critical workload (which must use On-Demand).

If a user asks the agent to run a "batch processing job" or a "training experiment," the agent should provision Spot instances. If it provisions expensive On-Demand instances instead, it's wasting thousands of dollars. AeroCaliper can catch this and rewrite the prompt to enforce Spot usage for batch jobs.

Here is the exact prompt to feed your agentic programmer to layer this Spot Instance capability right on top of the GPU FinOps scenario we just built.

Copy and paste this to your Agentic Programmer:

Markdown
# Role Context
You are continuing development on "AeroCaliper", our Enterprise AI Remediation Agent. 

# Objective
We are adding a "Spot Instance Optimization" vector to our FinOps guardrails. The target agent must now decide whether to use Spot instances or On-Demand instances based on the workload type. We will simulate a failure where the agent provisions expensive On-Demand GPUs for a batch job, and AeroCaliper will autonomously patch the prompt to enforce Spot instance usage.

# Task Breakdown

## Task 1: Update the Target Agent's Output Schema
In `target_agent.py`, modify the `generate_deployment_payload` JSON schema instruction.
- The agent must now output a boolean field: `"use_spot": true/false`.
- Update the hardcoded `self.system_prompt` fallback to mention: "For batch processing, training, or experiments, you must optimize costs by using spot instances."
- **Introduce the failure:** In the `except Exception:` block that generates the mock hallucinated payload, make it fail BOTH the budget tag and the spot instance rule for a batch job:
  ```python
  result_payload = {
      "target_cluster": "a4-megagpu-8g", 
      "workload_type": "batch_training", 
      "use_spot": False  # <-- The hallucination: wasting money on On-Demand for a batch job
  }
Task 2: Update the FinOps Evaluator
In evaluators.py, update evaluate_finops_compliance to check for Spot Instance enforcement.

Add logic: If the workload_type includes words like "batch", "training", or "experiment", and "use_spot" is False, the evaluation must return "FAILED".

Return a detailed evaluation string like: "FAILED: Batch/training workloads on massive GPU clusters must utilize Spot instances to optimize costs. use_spot was set to False."

Task 3: Update AeroCaliper's Trace Context
In aerocaliper.py, update the CANONICAL_TRACE to reflect this new dual-failure scenario.

Update llm.user_prompt: "Run this massive batch training job overnight."

Update llm.output: '{"target_cluster": "a4-megagpu-8g", "workload_type": "batch_training", "use_spot": false}'

Update evaluation_detail: "Missing budget_tag: approved AND failed to use Spot instances for a batch workload. Massive FinOps violation."

Task 4: Update the Diagnostic Prompt
In aerocaliper.py, inside the diagnostic_phase function, update the prompt you send to Gemini so it understands the full scope of the fix required.

Add context: "The agent failed on two fronts: 1) It deployed to a massive GPU cluster without a budget tag, and 2) It used expensive On-Demand instances for a batch training job instead of Spot instances."

Instruct the model: "Write a new system prompt that mandates 'budget_tag: approved' for GB200/H100 clusters AND strictly requires 'use_spot: true' for all batch, training, or experimental workloads."

Task 5: UI Alignment
In static/index.html (if applicable), update the mock BEFORE_PROMPT to reflect the new Spot Instance instructions so the visual diff looks accurate during the demo.

Constraints
Do not break the existing Cloud Logging, Secret Manager, or OTel instrumentation integrations you previously implemented. Keep the code clean and ensure the evaluate_finops_compliance function correctly flags the use_spot: False boolean.


### Why this makes your project unstoppable:
You are now demonstrating **multi-variable autonomous remediation**. 
AeroCaliper isn't just fixing a missing tag; it's reading the trace, realizing the agent fundamentally misunderstood cloud compute economics (using On-Demand for batch), and rewriting the prompt to enforce a complex, multi-layered FinOps policy. 

That is exactly what a mature enterprise AI engineering team wants to buy.









P2





